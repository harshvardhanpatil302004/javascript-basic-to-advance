"use strict";// treat all JS code as newer version. mostly done by default by browser

// alert(3+3)// in browser the output will come because JS engine is in browser, but running code in VS studio will not show output because it is not a browser. it is a text editor.

let name="abc"
let age=18
let isLoggedIn= false
let state=null
let a;// undefined
/* Data types:
number
bigint
string=""
boolean=true/false
null= standalone value
undefined
symbol*/

console.log(typeof null);// output: object. 
console.log(undefined);// output: undefined



